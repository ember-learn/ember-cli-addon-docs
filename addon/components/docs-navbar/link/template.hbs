{{#link-to route
  class=(concat
    'transition uppercase text-xs font-medium
    text-black no-underline mr-8 '
    (if (and (not isActive) (not (eq route 'index'))) 'opacity-50 hover:opacity-100')
  )
}}
  {{yield}}
{{/link-to}}
